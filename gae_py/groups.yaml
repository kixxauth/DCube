#
# A list of datastore interfaces and the groups that have permission on each one.
# Used by the gate module (gate.py)
#

create_new_user:
- users
get_public_user:
- users
delete_user:
- users
get_chap_user_creds:
- ROOT
get_user_groups:
- ROOT
update_chap_user_creds:
- ROOT
